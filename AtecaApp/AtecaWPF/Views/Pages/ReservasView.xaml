<Page x:Class="AtecaWPF.Views.Pages.ReservasView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AtecaWPF.Views.Pages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ReservasView">

    <Grid>
    <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Button Content="Recargar Reservas"
                Command="{Binding CargarReservasCommand}"
                Margin="10"/>

        <DataGrid Grid.Row="1"
          ItemsSource="{Binding Reservas}"
          AutoGenerateColumns="False"
          Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" />
                <DataGridTextColumn Header="Profesor" Binding="{Binding ProfesorNombre}" />
                <DataGridTextColumn Header="Grupo" Binding="{Binding GrupoClaseNombre}" />
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" />
                <DataGridTextColumn Header="Fecha Solicitud" Binding="{Binding FechaSolicitud}" />
                <DataGridTextColumn Header="Fecha Resolucion" Binding="{Binding FechaResolucion}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
