<Page x:Class="Examen2Evaluacion.Views.Pages.PedidosView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Examen2Evaluacion.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PedidosView">

    <Grid>
        <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <!-- Lista de pedidos -->
        <ListBox ItemsSource="{Binding Pedidos}"
         SelectedItem="{Binding PedidoSeleccionado, Mode=TwoWay}"
         Margin="10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Id, StringFormat='ID: {0}'}"/>
                        <TextBlock Text="{Binding Fecha, StringFormat='Fecha: {0:yyyy-MM-dd}'}"/>
                        <TextBlock Text="{Binding UsuarioNombre, StringFormat='Usuario: {0}'}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <!-- Vista lateral de productos del pedido -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Productos del Pedido" FontWeight="Bold" FontSize="16"/>
            <ItemsControl ItemsSource="{Binding PedidoSeleccionado.Productos}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}" />
                            <Button Content="Eliminar" Command="{Binding DataContext.EliminarProductoCommand, RelativeSource={RelativeSource AncestorType=Page}}" CommandParameter="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ComboBox ItemsSource="{Binding TodosLosProductos}"
                  DisplayMemberPath="Nombre"
                  SelectedItem="{Binding ProductoSeleccionado}"
                  Margin="0,10"/>
            <Button Content="Agregar Producto"
                Command="{Binding AgregarProductoCommand}"
                Margin="0,5"/>
        </StackPanel>
    </Grid>

</Page>
